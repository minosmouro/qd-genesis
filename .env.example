# Database Configuration# Configurações de Ambiente para QuadraDois CRM

DATABASE_URL=postgresql://postgres:CHANGE_ME_PASSWORD@postgres:5432/gandalf_db

DATABASE_HOST=postgres# ========== TIMEZONE ==========

DATABASE_PORT=5432TZ=America/Sao_Paulo

DATABASE_USER=postgres

DATABASE_PASSWORD=CHANGE_ME_PASSWORD# ========== DATABASE ==========

DATABASE_NAME=gandalf_dbDATABASE_USER=gandalf_prod

DATABASE_PASSWORD=CHANGE_ME_strong_password_min_16_chars

FERNET_KEY=CHANGE_ME_GENERATE_WITH_PYTHONDATABASE_NAME=gandalf_db

SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSLDATABASE_URL=postgresql://gandalf_prod:CHANGE_ME_strong_password_min_16_chars@postgres:5432/gandalf_db

FLASK_DEBUG=False

# ========== REDIS ==========

# Redis ConfigurationREDIS_URL=redis://redis:6379/0

REDIS_URL=redis://redis:6379/0

# ========== SECRETS (GERAR NOVOS EM PRODUÇÃO!) ==========

# OpenAI Configuration# Gerar SECRET_KEY: openssl rand -hex 32

OPENAI_API_KEY=your-openai-api-key-hereSECRET_KEY=CHANGE_ME_run_openssl_rand_hex_32



# Qdrant Configuration# Gerar FERNET_KEY: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

QDRANT_URL=http://qdrant:6333FERNET_KEY=CHANGE_ME_run_python_fernet_generate_key



# CORS Configuration for Business Center# ========== AWS S3 (Upload de Imagens) ==========

CORS_ORIGINS=http://localhost:3000,http://localhost:4000,http://localhost:5173,https://app.quadradois.com.br,https://crm.quadradois.com.br,https://api.quadradois.com.brAWS_ACCESS_KEY_ID=your_aws_access_key_here

AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here

# AWS Configuration (para upload de imagens)AWS_S3_REGION=us-east-1

AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_HEREAWS_S3_BUCKET_NAME=quadra-fotos-prod

AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_KEY_HERE

AWS_S3_REGION=us-east-2# ========== CANALPRO INTEGRATION (OPCIONAL - FALLBACK) ==========

AWS_S3_BUCKET_NAME=quadra-fotos# NOTA: Estas variáveis são usadas apenas como FALLBACK

# Cada tenant deve configurar suas próprias credenciais CanalPro via interface
# O sistema primeiro usa as credenciais do tenant, depois estas (se configuradas)
# Recomendado: Deixar vazio para forçar configuração por tenant
GANDALF_PUBLISHER_ID=
GANDALF_ODIN_ID=
GANDALF_CONTRACT_ID=
GANDALF_CLIENT_ID=CANALPRO_WEB

# ========== CORS (Domínios Permitidos) ==========
# Separar múltiplos domínios por vírgula, SEM espaços
# Produção:
CORS_ORIGINS=https://app.quadradois.com.br,https://crm.quadradois.com.br
# Desenvolvimento local (descomente se for usar):
# CORS_ORIGINS=http://localhost:4000,http://localhost:5173

# ========== CELERY (Processamento Background) ==========
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# ========== FLASK ==========
FLASK_ENV=production
FLASK_DEBUG=False

# ========== QUEUE CONFIGURATION ==========
REFRESH_QUEUE_MAX_BATCH_SIZE=30
REFRESH_QUEUE_TIMEOUT_SECONDS=300
REFRESH_QUEUE_MAX_RETRIES=3
